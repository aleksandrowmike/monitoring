import { ReplaceValueWithTextPipe } from './replace-value-with-text.pipe';

describe('ReplaceValueWithTextPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceValueWithTextPipe();
    expect(pipe).toBeTruthy();
  });
});
